const initialState = {
  dataAccount: [],
  isLoading: false
};

const getUser = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default getUser;
